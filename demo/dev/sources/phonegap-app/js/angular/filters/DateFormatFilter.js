/*meApp.filter('DateFormat', function() {
    return function(input) {
        if (input == null)
            return input;

        input = input.toString().split('-');

        if (input[0].length == 4)
            input = input.reverse();

        return input.join('-');
    };
});*/